name: backgroundsai
description: Backgrounds for Android
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  bloc: ^8.1.1
  cached_network_image: ^3.2.3
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.2
  flutter_localizations:
    sdk: flutter
  
  intl: ^0.17.0
  repo:
    path: packages/repo

dev_dependencies:
  
  very_good_analysis: ^3.1.0

flutter:
  uses-material-design: true
  generate: true
